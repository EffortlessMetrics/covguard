//! Backward-compatible façade for `covguard-orchestrator`.
//!
//! `covguard-app` is retained as a stable integration surface while the
//! orchestration layer is maintained in `covguard-orchestrator`.

pub use covguard_orchestrator::*;
