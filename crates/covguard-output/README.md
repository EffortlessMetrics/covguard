# covguard-output

Output rendering utilities for covguard reports.

## Responsibilities

- Re-export canonical markdown/annotation/SARIF rendering from `covguard-render`.
- Define and expose output feature budgets and truncation semantics.
- Keep rendering behavior consistent across app and CLI outputs.
